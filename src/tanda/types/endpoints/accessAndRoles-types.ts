// param

// body

// response

export interface AccessAndRolesResponse {
  id: number;
  name: string;
  description: string;
  type: string;
  enabled: boolean;
  native: boolean;
  sort_order: number;
};

// ad hoc