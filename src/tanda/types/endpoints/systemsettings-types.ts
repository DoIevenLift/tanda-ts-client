//param

//body

//response
//! these are odd. Depends on the type of user. No full list of response.
export interface SystemSettingsResponse {
  enable_availability?: boolean;
  unavailability_minimum_date?: string;
  enable_employee_leave?: boolean;
  leave_request_default_length?: number;
}

//adhoc