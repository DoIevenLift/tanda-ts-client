{
  "name": "tanda-js",
  "version": "0.0.11",
  "description": "A typescript client for interacting with the Tanda v2.0 API",
  "main": "index.js",
  "author": "Jacob",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "ts-node": "^10.9.1",
    "tsup": "^8.0.1",
    "typescript": "^5.3.2"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts-resolve --minify --clean --sourcemap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@swc/core": "^1.3.99"
  }
}
